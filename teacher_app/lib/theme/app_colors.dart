import 'package:flutter/material.dart';

/// Identidade visual Mitaka Edu. Sem degradês — melhor contraste e menos estímulo.
abstract final class AppColors {
  static const navy = Color(0xFF0D1B2A);
  static const purple = Color(0xFF3A2F6E);
  static const purpleSoft = Color(0xFFEEEAF8);

  static const cream = Color(0xFFF7F9FB);
  static const surface = Color(0xFFFFFFFF);
  static const ink = Color(0xFF0D1B2A);
  static const muted = Color(0xFF5C6B7A);
  static const line = Color(0xFFE4E8EE);

  /// Acento vivo da marca (ícones, destaques, gráficos).
  static const brand = Color(0xFF00BFA5);
  /// Teal acessível para botões e texto sobre fundo claro.
  static const brandDark = Color(0xFF00796B);
  static const brandSoft = Color(0xFFE6FAF7);

  static const ok = Color(0xFF2F7D4A);
  static const okSoft = Color(0xFFE8F5EC);
  static const attention = Color(0xFFB7791F);
  static const attentionSoft = Color(0xFFF8EFDE);
  static const pending = Color(0xFF6B7280);
  static const pendingSoft = Color(0xFFEEF0F2);

  static const coral = Color(0xFFE07A5F);
  static const coralSoft = Color(0xFFFBECE7);
  static const sky = Color(0xFF4DB6E2);
  static const skySoft = Color(0xFFE8F6FB);
  static const sun = Color(0xFFE2B93B);
  static const sunSoft = Color(0xFFFBF4DE);
  static const grape = Color(0xFF3A2F6E);
  static const grapeSoft = Color(0xFFEEEAF8);
  static const lavender = Color(0xFFA78BFA);

  static const highContrastBg = Color(0xFF000000);
  static const highContrastFg = Color(0xFFFFFFF5);
  static const highContrastAccent = Color(0xFFFFD60A);
  static const highContrastSoft = Color(0xFF1A1A1A);
}
